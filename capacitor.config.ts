import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.horizonmode.gymscreens",
  appName: "gymscreens",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
